donate:
  url:   /donate
  param: { module: donate, action: index }

donate_time:
  url:   /donate/time
  param: { module: donate, action: time }
  
donate_material:
  url:   /donate/stuff
  param: { module: donate, action: material }

donate_housing:
  url:   /donate/place
  param: { module: donate, action: housing }

need:
  url:   /need
  param: { module: need, action: index }

need_time:
  url:   /need/time
  param: { module: need, action: time }
  
need_material:
  url:   /need/stuff
  param: { module: need, action: material }

need_housing:
  url:   /need/place
  param: { module: need, action: housing }

# default rules
homepage:
  url:   /
  param: { module: default, action: index }

default_index:
  url:   /:module
  param: { action: index }

default:
  url:   /:module/:action/*
